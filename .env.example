# .env.example

#
# Author: Michael Welter <me@mikinho.com> - https://github.com/mikinho
#

# Optional local extended client-neutrality audit pattern (POSIX ERE).
# Keep concrete private identifiers only in the ignored .env file.
# The scanner reads assignments as data, never as shell commands. Use a
# single-line value of at most 4095 UTF-8 bytes; quotes and a following comment
# are supported. Quote any value that itself contains whitespace followed by
# '#', because an unquoted remainder from that point is read as the comment.
# Backslashes are preserved literally, and shell/variable expansion is never
# performed. A leading UTF-8 byte-order mark is ignored.
# An exported environment value takes precedence, including an empty value.
CONFIG_PRIVATE_IDENTIFIER_PATTERN=

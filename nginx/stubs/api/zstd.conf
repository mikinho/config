# nginx/stubs/api/zstd.conf

#
# Author: Michael Welter <me@mikinho.com> - https://github.com/mikinho
#

# Optional: prefer zstd for API responses.
# API locations include stubs/api/*.conf; the level, size floor, and media
# types come from stubs/http/zstd.conf. Authenticated and token-bearing routes
# still need the compression review described in the nginx README.
zstd on;

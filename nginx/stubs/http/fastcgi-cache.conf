# stubs/http/fastcgi-cache.conf

#
# Author: Michael Welter <me@mikinho.com> - https://github.com/mikinho
#

# Optional: WordPress FastCGI cache support.
# Install this stub only on hosts that use includes/wordpress-cache.conf.
fastcgi_cache_path /var/lib/nginx/fastcgi levels=1:2 keys_zone=wp:100m inactive=60m max_size=1g;
fastcgi_cache_key "$scheme$request_method$host$request_uri";

map $http_cookie $wp_logged_in {
    default                  0;
    "~*wordpress_logged_in_" 1;
    "~*comment_author_"      1;
    "~*wp-postpass_"         1;
}

map $request_method $wp_nocache_method {
    default 0;
    POST    1;
    PUT     1;
    DELETE  1;
}

map $request_uri $wp_nocache_uri {
    default            0;
    "~*/wp-admin/"     1;
    "~*/wp-json/"      1;
    "~*/wp-[^/]*\.php" 1;
    "~*/feed/?$"       1;
    "~*sitemap.*\.xml" 1;
    "~*preview=true"   1;
}
